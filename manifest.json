{
    "manifest_version": 3,
    "name": "Tab Closer Extension",
    "version": "1.0",
    "description": "Saves the tab data into a html file and closes the most recently opened tabs if the total number of tabs exceeds 30.",
    "permissions": ["tabs", "downloads"],
    "background": {
      "service_worker": "background.js"
    },
    "action": {
      "default_popup": "popup.html"
    },
    "host_permissions": ["<all_urls>"]
  }
  